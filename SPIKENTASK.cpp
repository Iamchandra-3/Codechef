#include<iostream>
#include<string.h>
using namespace std;

int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n;
        cin>>n;
        string s;
        cin>>s;
        //input done
        for(int i=0;i<n;i++)
        {
            if((s[i]==0 && s[i+1] ==0)||(s[i]==1&&s[i+1]==1)
            {
                s[i+1]
            }
        }
    }
}